var filme = false; //testando !

if (!filme) {   // a exclamação testará se o filme é null, undefined ou false.
    console.log ("Filme inexistente.")
} else {
    console.log ("Você encontrou o filme.")
}